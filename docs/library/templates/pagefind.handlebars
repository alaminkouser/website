<!doctype html>
<html lang="en">
  <head>
    <title>{{title}}</title>
    <meta data-pagefind-default-meta="keywords:{{keywords}}" />
  </head>
  <body>
    <h1>{{title}}</h1>
    {{{body}}}
  </body>
</html>